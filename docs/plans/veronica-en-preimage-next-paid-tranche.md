# Veronica English next paid tranche (proposal only)

No operation in this plan is authorized or executed by this remediation run.

## Preconditions

1. Explicitly renew the stale source workspaces together with their selected audio and timing provenance.
2. Resolve the three deterministic diversity blockers without relaxing source-grounding or opening-novelty gates.
3. Keep the historic census ledger intact: 4 requests / `$0.069837`.

## Smallest useful tranche

Use one representative newly provisioned short with deterministic readiness plus `01a-revenue-is-not-a-good-business` only if its existing admission and cache identity remain valid. Execute current cheap primary batched scene QA; proceed to beat QA only after scene PASS. Do not escalate a REVIEW in this diagnostic tranche.

| Limit | Proposal |
|---|---:|
| Provider | Current configured cheap primary QA model/policy |
| Expected requests | 2–3 batched requests |
| Hard maximum | 3 requests |
| Estimated cost | `COST_UNRESOLVED` until current execution policy prices the exact model/token ceilings |
| Conservative ceiling | €0.25, subject to scheduler preflight and overall €2.50 budget |

## Success / stop conditions

Success: cache-safe scene and beat coverage for the representative, durable request accounting, and no artifact mutation. Stop before dispatch if pricing, admission, cache identity, or projected budget is unavailable; stop after the first REVIEW/BLOCK with its issue family recorded. No TTS, image, rendering, publication, or playlist operations are part of this tranche.
