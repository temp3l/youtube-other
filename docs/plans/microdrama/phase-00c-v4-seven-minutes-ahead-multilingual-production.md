# Phase 00C-V4 — `7 MINUTES AHEAD` multilingual production profile

Date: 2026-08-12
Status: ACTIVE / CORPUS READY

## Executive recommendation

Adopt `content-packs/seven-minutes-ahead-content-pack-v4-multilingual/`
as the first production profile and deterministic regression corpus. It has one
shared 100-episode narrative canon and 400 approved localized script artifacts.
Script completeness does not authorize mass TTS, rendering, or publication.

This plan supersedes V3 production assumptions for this series. No persisted
V3-specific Phase 00C document was found; the V3 content pack remains lineage
evidence, not the current production corpus.

## Verified inventory and integrity

| Area | Files | Episodes |
|---|---:|---:|
| Root README | 1 | 0 |
| `languages/en` | 106 | 100 |
| `languages/de` | 106 | 100 |
| `languages/es` | 106 | 100 |
| `languages/pt-BR` | 106 | 100 |
| `qa` | 5 | 0 |
| `shared` | 3 | 0 |
| Total | 433 | 400 locale variants |

The pack contains 418 Markdown, 8 JSON, 6 CSV, and 1 JSONL files. Every
locale contains unique E001–E100 records. No symlinks, unsafe paths, missing
declared files, or unexpected top-level files were found.

`qa/sha256.json` covers every file except itself: 432/432 hashes recompute with
zero mismatches. Its own SHA-256 is
`8ce49e66a6397500941b5109246019971858f321b154fab579335848f3d56dc6`.
The `sha256-v1` pack manifest hash is
`7706287b9db0495c32109aa5896aff9dc08149d0adaa9222f7c50120cf6dde5c`,
computed over sorted UTF-8 `path<TAB>digest<LF>` records.

All 400 manifest JSON/CSV, timing, editorial, and Markdown metadata records
agree. Numeric spellings such as `60` versus `60.0` are equivalent and are the
only serialization differences.

## Identity and authority

Stable pack identity:

```text
seriesId: seven-minutes-ahead
packVersion: v4-multilingual
canonical episodes: E001–E100
locales: en-US, de-DE, es-ES, pt-BR
```

Folder names `en`, `de`, `es`, and `pt-BR` are source-path aliases, not runtime
locale identity.

```text
approved EN-v3 continuity lineage
  -> language-neutral structured series/episode state
    -> V4 imported locale ScriptRevision
      -> locale production artifacts
```

All four E001 scripts reference the same canonical narrative episode. Localized
titles and prose are metadata/revisions, not episode identity. V4 scripts enter
as `IMPORTED_APPROVED_LOCALIZED_SCRIPT`; they are not regenerated by default.

### Source-of-truth matrix

| Concern | Authority | Secondary evidence | Runtime projection |
|---|---|---|---|
| Series canon | EN-v3 lineage, shared audit/global truth | locale bibles | accepted language-neutral SeriesBible revision |
| Episode continuity | shared episode state and audit constraints | EN script | NarrativeSnapshot/EpisodeBoundary |
| EN/DE/ES/PT-BR script | matching locale episode Markdown | locale manifest | locale ScriptRevision |
| Lexical WPM | V4 locale profile/manifest | root and locale READMEs | `LEXICAL_GATE` |
| Lexical duration | words divided by locale WPM | timing QA | lexical admission evidence |
| Editorial score | QA editorial artifacts | manifest/Markdown | immutable craft QA evidence |
| Final timing | selected locale audio and alignment | lexical estimate | canonical locale timing |
| Visual identity | approved semantic registry/shot IDs | canon and script | shared AssetRevision references |
| Signal UI | typed language-neutral `SignalUIState` | locale script | locale compositor projection |
| Subtitles | selected locale audio/alignment | locale script | locale subtitle artifact |
| Metadata | provider+locale MetadataRevision | episode/script metadata | platform projection |
| Publication | durable intent/attempt/receipt | render/metadata revisions | provider/account/locale state |

### Shared-file classification

- `canonical-continuity-audit-en-v3.md`: chronology fixes and the E100 Season 2
  boundary are canonical constraints needing structured IDs; PASS text is QA
  evidence; EN-v3 is heritage/provenance.
- `episode-state.jsonl`: `episode` and `arc` are canonical identity; prose
  `new_information` and `open_loop` need fact, reveal, promise, and boundary
  structuring. It is not a localized script or timing source.
- `series-state.json`: premise, characters, global truth, and knowledge needs
  seed the SeriesBible and typed canon. `target_spoken_wpm:160`, V3 range, and
  V3 average are heritage only. The selected-audio authority flag is active.

The audit's E024, E038, E088–E089, Nora deception, Clara anchor, version/Return,
AHEAD bootstrap, and E100 `+7 YEARS` rules must survive import. Intentional
deception is not a continuity error. E100 closes Season 1 while preserving a
non-retroactive Season 2 hook.

## Timing and QA

| Locale | WPM | Word gate | Words avg | Seconds min–max | Seconds avg | Timing | Editorial min–max/avg |
|---|---:|---:|---:|---:|---:|---:|---:|
| en-US | 155 | 145–160 | 153.88 | 58.06–61.94 | 59.57 | 100/100 | 9.75–9.82 / 9.800 |
| de-DE | 150 | 140–155 | 146.24 | 56.00–62.00 | 58.50 | 100/100 | 9.73–9.82 / 9.786 |
| es-ES | 155 | 145–160 | 152.58 | 56.13–61.94 | 59.06 | 100/100 | 9.72–9.81 / 9.777 |
| pt-BR | 155 | 145–160 | 152.47 | 56.13–61.94 | 59.02 | 100/100 | 9.73–9.82 / 9.788 |

All 400 variants pass lexical timing and the `>=9.50` editorial gate. Editorial
scores are expert craft evidence, not empirical virality predictions.

The V4 locale profile wins if `shared/series-state.json` says 160 WPM while an
EN manifest says 155 WPM. The shared value is deprecated as V4 runtime input.
After TTS, selected audio plus measured alignment wins over every lexical
estimate. `LEXICAL_GATE` and `AUDIO_GATE` are separate policies; audio soft/hard
limits remain uncalibrated until the E001–E003 canary.

## Import boundary

The importer preserves raw source/hash provenance and produces typed
`SeriesImport`, 100 `EpisodeImport`, 400 `LocalizedScriptImport`, QA artifacts,
and continuity artifacts. The immutable import record includes import ID,
series ID, pack version, source root, manifest hash/hash-set revision, locales,
episode count, timestamp, and validation status.

Reject unsafe/absolute/traversal paths, symlinks, invalid hashes, missing or
unexpected required files, duplicate/missing IDs, unknown locales, malformed
metadata, manifest/episode disagreement, unresolved source lineage, or runtime
authority conflicts. Preserve raw Markdown for evidence but never make headings
the runtime domain model.

The German E001/E002 full stories preserve the canonical outgoing information,
but their dedicated cliffhanger fields narrow the unseen-woman/shared-receiver
boundary. Treat this as a semantic-admission warning requiring explicit
EpisodeBoundary adjudication, not automatic rewriting or an integrity failure.

## Narrative and localization contracts

Each episode compiles a language-neutral `EpisodeBoundaryContract` containing
episode ID, outgoing state, audience and character knowledge, critical props,
cliffhanger semantic ID, and next-opening obligation. Hook projections retain
canonical intent while allowing idiomatic wording.

Protected terms:

- `DO_NOT_TRANSLATE`: character names, `7 MINUTES AHEAD`, `AHEAD`, `Return`,
  episode/receiver IDs, and V01–V18 identifiers.
- `FIXED_LOCALIZATION`: Signal (`Signal`, `Signal`, `Señal`, `Sinal`) and
  reviewed term families for receiver, observer, relay, anchor, annex, packet,
  countdown, and protocol.
- `CONTEXTUAL_LOCALIZATION`: ordinary location, action, and dialogue phrasing.

Future locales use canonical state, a canonical source script, and glossary to
produce a draft, deterministic QA, semantic parity QA, and an accepted revision.
They do not alter imported V4 revisions.

## Production profile and media reuse

`SevenMinutesAheadProductionProfile` owns series/pack identity, 9:16, supported
locales, default locale, speech profiles, runtime intent, visual policy, Signal
UI template, lip-sync policy, source-asset policy, editorial gate, timing policy,
and publication-target policy. These values are not generic constants.

- Shared canon/Beat/Scene/Shot semantic IDs and approved visual assets are the
  default across locales.
- Locale outputs own TTS, alignment, timing, subtitles, UI text, metadata, and
  final render/composition.
- A visual locale fork needs a recorded baked-text, semantic, cultural,
  legal/platform, or timing reason.
- V4 contains no visual registry. Seed future registry work from the eight
  named characters without inventing assets.
- E001–E003 should start with 5–7 strong source plates per episode and 8–12
  editorial cuts; this is a canary budget, not an engine constant.
- Use stable per-character/per-locale voice profiles and mixed narration/dialogue
  segmentation. Prefer reaction, listening, insert, phone, offscreen, and
  controlled-mouth shots over mandatory multilingual lip sync.
- Readable Signal/phone text is compositor-owned typed UI, never image-model text.
- Subtitle timing and line grouping derive independently from selected locale
  audio and platform-aware safe zones.

Shared visual cache keys exclude locale only when semantics contain no localized
content. TTS, subtitle, UI, metadata, timing, and render keys include locale.
Wording-only changes invalidate that locale downstream; canon beat changes may
invalidate all locales; character-reference changes invalidate affected shared
visuals. Shared visuals are costed once, while localized audio/render work is
costed by episode, locale, provider, asset type, and revision.

## Hook, pacing, and canary conclusion

E001–E010 preserve direct imminent-danger, context-trap, secret, competing-future,
moral-command, hidden-connection, reciprocal-threat, authority, dead-receiver,
and trusted-friend hooks across all locales. Their cliffhangers preserve the
canonical outgoing boundary subject to the German metadata warning above.

Use an initial semantic pacing grammar of 0–3s hook, 3–12s orientation, 12–35s
escalation/action, 35–50s reversal, and 50–60s information-boundary cliffhanger.
Selected locale audio determines real timestamps.

Rollout remains bounded:

1. Import/integrity and narrative admission.
2. EN E001–E003 TTS/timing, then visual production; no publication.
3. DE/ES/PT-BR E001–E003 using shared visual identity.
4. E004–E010 only after both canaries pass.
5. Separate private and audited public provider canaries.
6. E011+ only after production and performance evidence.

No command may turn 400 scripts into 400 TTS files, renders, or publication jobs
without bounded task scope and explicit side-effect authorization.

## Publication and analytics

`PublicationTargetProfile` maps series+locale+provider to an exact account,
metadata profile, schedule profile, and enabled flag. TikTok and YouTube metadata
are separate projections. Store schedule instants in UTC and never derive an
account or timezone from locale alone. AI-content disclosure follows media
provenance, not caption language.

Analytics observations retain canonical episode, locale, provider, account,
publication, render/metadata revision, and observation window. Cross-locale
comparisons annotate account baseline, audience, publish time, and experiment
context. They are observational unless controlled assignment supports causality
and never write canon directly.

## Readiness and risks

- `V4_INTEGRITY_READY`: satisfied.
- `V4_NARRATIVE_IMPORT_READY`: pending importer, structured canon, and German
  E001/E002 boundary admission.
- Media, multilingual, E004–E010, publication, and E011+ gates: blocked on their
  explicit backlog dependencies and approvals.

Blocking implementation risk is absence of the embedded microdrama store and
Narrative Core. High risks are locale identity loss, baked English UI, wrong
account publication, and locale cache collisions. Medium risks include prose
canon structuring, cliffhanger metadata narrowing, uncalibrated audio gates,
and subtitle/safe-zone quality.
