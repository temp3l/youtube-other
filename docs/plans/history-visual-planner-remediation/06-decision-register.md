# Decisions requiring explicit approval

| Decision | Options | Recommended option | Rationale | Trade-offs | Compatibility impact | Approval required from |
| --- | --- | --- | --- | --- | --- | --- |
| Timing source of truth | metadata target; WPM estimate; measured total audio; word timestamps | staged: word timestamps → measured total → estimate | preserves narration and aligns render to spoken audio | post-audio reconciliation/reapproval complexity | new History workflow revision and artifacts | pipeline architect + showrunner |
| Semantic beat versus shot model | one beat=one shot; scene-only; separate units/beats/assets/shots | separate five concepts described in target design | removes current count/timing ambiguity and supports reuse | more typed artifacts and adapter | v2 History plan; leave v1 read-only | pipeline architect + visual director |
| Immutable boundary point | before timing; after timing; only renderer | after timing + semantic validation | visual selection cannot silently alter narration | requires a revision for timing changes | new plan identity | pipeline architect |
| Schema/versioning | mutate v1; tolerant optional fields; v2 envelope + reader | versioned `history-visual-plan.v2` with v1 legacy reader | avoids reinterpretation of approved artifacts | temporary dual-path code | no v1 hash/approval migration | architect + release owner |
| Blocking validation policy | warnings only; structural errors only; semantic gate | block integrity/timing/required-variant/stale-derivative failures | approval must mean renderable, complete plan | more drafts require intervention | CLI can return new blocked status | showrunner + production owner |
| Estimate-only approval | prohibit; allow with warning; allow only plan review | permit only explicitly provisional plan review, not final media approval | makes uncertainty visible without blocking early editorial work | adds approval scope/state | History-only policy addition | showrunner + production owner |
| Media taxonomy | retain four types; unbounded free strings; bounded History union | bounded History union including evidence/reconstruction/map-state/diagram-state forms | supports documentary authority and deterministic validation | initial taxonomy governance | History profile only | history visual director |
| Media-mix configuration | global fixed percentages; LLM chooses freely; profile+episode soft goals | profile defaults with episode/section overrides and hard semantic constraints | prevents cycle and genre leakage | configuration surface | new History policy version/hash | visual director + product owner |
| LLM responsibility | fully deterministic; LLM chooses final plan; schema-constrained enrichment | deterministic policy/validation and optional LLM enrichment | preserves auditability while allowing creative details | LLM output needs review/cost controls | prompt version is an explicit input | architect + AI owner |
| Aspect-ratio ownership | renderer crops; image prompt only; composition planner owns variants | shared semantic intent, History composition owns ratio-specific variants, renderer realizes them | supports maps/text/vertical composition safely | two variants increase planning work | additive adapter; current 16:9 plan remains legacy | visual director + rendering owner |
| Map/diagram representation | static assets; generic video; master/state sequences | typed reusable master + immutable states | distinguishes reuse from narrative change | cartographic data/model work | new asset-state contracts/hashes | history visual director + historian |
| Provenance requirements | optional text note; required for all; required by media discriminant | required for archival/document/quotation; constraints required for reconstructions | protects evidence claims without pretending cinematic images are evidence | source/rights gaps can block a beat | new evidence linkage; legacy unaffected | historian/editorial standards + legal/rights owner |
| Historical uncertainty | prose only; confidence number only; typed claim + disclosure | claim classification/confidence/source IDs plus display directive | makes disputed interpretations reviewable | needs claim extraction quality | History schema/approval display | historian/editorial standards |
| Cache invalidation | reuse v1 hashes; blanket delete; schema/policy-aware new identity | new versioned identities, legacy read-only, no automatic deletion | protects approved artifacts and reproducibility | cache duplication | explicit misses on v2 only | architect + operations owner |
| Legacy plan handling | auto-upgrade; refuse all; preserve/read-only plus explicit replan | preserve/read-only and require explicit v2 replan | avoids silently changing approved meanings | operators manage two versions temporarily | compatible inspection, no silent authorization | release owner + production owner |
| Shared-package changes | shared rewrite; History fork; History-first adapter | History-first additive adapter, promote only proven generic pieces | limits Dark Truth/math/generic blast radius | eventual duplicate adapter code | non-History behavior unchanged by default | architect + maintainers of affected packages |
| Script-source mismatch response | silently use script; overwrite from pack; block and audit | block new approval, retain both, require lineage resolution | current pack/script counts disagree without provenance | may halt a History draft | new import integrity diagnostic | content owner + production owner |
| Approval scope | plan hash only; plan+derivative; full generated assets | semantic plan + renderable derivative hashes initially | current gate does not bind consumer | may require an extra planning task | new approval record v2 | pipeline architect + production owner |

## Approval sequence

Before WP0: approve timing policy, semantic blocking policy, v2/legacy stance, and script-lineage response. Before WP1/WP2: approve concept ownership and provisional-versus-final approval semantics. Before WP3–WP5: approve taxonomy, media-mix policy, LLM role, provenance/uncertainty policy. Before WP6/WP7: approve ratio ownership, adapter scope, cache policy and legacy rollout.
