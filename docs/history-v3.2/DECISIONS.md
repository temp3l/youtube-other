# History V3.2 Decisions

- V3.2 is History-only and explicit; existing planners and defaults remain unchanged.
- Napoleon and Fall of Rome are `long`; Black Death remains `standard`.
- Canonical scripts remain unchanged. The three audited spoken-word/duration metadata entries are corrected.
- Candidate research sources and model suggestions cannot establish support. Missing human-verified evidence blocks content approval.
- Missing immutable measured narration audio blocks production approval.
- Unsupported diagrams fall back deterministically; V3.2 does not emit a weak template as factual evidence.
- A manifest-wide metadata edit does not revise an episode whose source hash is unchanged.
