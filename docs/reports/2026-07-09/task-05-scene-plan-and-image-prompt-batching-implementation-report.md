Source plan file path: `docs/plans/batch-orchestration/tasks/task-05-scene-plan-and-image-prompt-batching.md`
Date of execution: `2026-07-09`
Summary of implemented changes: Added canonical full `image-prompt` and `image-generation` workflow stages after English full `visual-model`; introduced typed `scene-plan-batch` and `image-prompt-batch` workflow artifacts plus a prompt-stage persistence helper; extended image-batch coverage to assert persisted full visual plans and scene manifests exist before batch JSONL generation.
Files changed: `packages/story-localization/src/story-workflow-planner.ts`; `packages/story-localization/src/story-workflow-visual.ts`; `packages/story-localization/src/story-workflow-visual.unit.test.ts`; `packages/story-localization/src/story-workflow.integration.test.ts`; `packages/image-generation/src/image-batch-planner.unit.test.ts`; this report.
Tasks completed: full canonical image-planning stages in workflow; typed scene-plan/image-prompt artifact lineage; persisted full scene-plan and prompt-plan verification.
Tasks partially completed: integration coverage was updated, but `pnpm test:focused` only executes its first file and did not run `story-workflow.integration.test.ts` in this turn.
Tasks not completed: no executable `stories production resume` runner wiring yet.
Deviations from the original plan: reused existing per-scene visual-plan and manifest persistence instead of introducing a new aggregate planning manifest.
Tests/checks run: `pnpm test:focused -- apps/cli/src/story-production-command.unit.test.ts apps/cli/src/story-localization-commands.unit.test.ts` failed with the pre-existing `stories` command-list expectation; `pnpm test:focused -- packages/story-localization/src/story-workflow-visual.unit.test.ts packages/image-generation/src/image-batch-planner.unit.test.ts` pass; `pnpm test:focused -- packages/story-localization/src/story-workflow-visual.unit.test.ts packages/story-localization/src/story-workflow.integration.test.ts packages/image-generation/src/image-batch-planner.unit.test.ts` pass for the first and third files only due wrapper behavior; `pnpm --filter @mediaforge/story-localization typecheck` pass; `git diff --check -- <changed files>` pass.
Test results: source changes compile; focused visual and image-batch tests pass; the updated integration file was not executed by the wrapper used here.
Known risks or follow-up work: run the integration file directly or improve `scripts/test-focused.sh` when the verification budget allows; later tasks still need image batch import/retry execution and render-readiness wiring.
Recommended next steps: continue with Task 06 image batch resilient import/retry on top of the new full-stage artifact lineage.
