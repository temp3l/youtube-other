# Codex Run: API Task 13

## Summary

Added the public webhook wire primitives: a versioned envelope with subject ordering fields and HMAC-SHA256 `v1` signatures. Verification rejects stale payloads and accepts overlapping signing secrets during rotation.

## Changed Paths

- `packages/application/src/{webhooks,index,contracts.unit.test}.ts`
- `docs/reports/codex-runs/2026-07-31-api-task-13.md`

## Tests

- `pnpm --filter @mediaforge/application build` — passed.
- Focused application contracts suite — passed (6 tests).

## Commit Hash

Base: `5cf1262`; changes remain uncommitted.

## Unresolved Risks

Endpoint persistence, SSRF-safe delivery, retry/dead-letter workers, replay APIs, and generated SDK distribution still require the Task 13 delivery implementation.
