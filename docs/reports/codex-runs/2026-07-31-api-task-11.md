# Codex Run: API Task 11

## Summary

Added tenant-scoped, expiring hashed pilot-key authentication primitives and made the HTTP adapter fail closed without an identity resolver. Requests require workspace ownership and route-appropriate permissions; foreign workspace IDs receive opaque 404 responses.

## Changed Paths

- `packages/application/src/{errors,tenant-identity,index,contracts.unit.test}.ts`
- `apps/api/src/{http-server,http-server.integration.test}.ts`
- `docs/reports/codex-runs/2026-07-31-api-task-11.md`

## Tests

- `pnpm --filter @mediaforge/application build` — passed.
- `pnpm --filter @mediaforge/api typecheck` — passed.
- Focused API integration suite — passed (4 tests).

## Commit Hash

Base: `5cf1262`; changes remain uncommitted.

## Unresolved Risks

OIDC/JWKS verification, encrypted credential handles, and production principal storage require deployment configuration and remain outside this in-process pilot-key adapter.
