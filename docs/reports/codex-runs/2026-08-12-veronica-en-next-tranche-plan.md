# Veronica EN next tranche

Tranche: `EN_SHORT_SEMANTIC_ADMISSION_REPAIR`.

Episodes: 03B first, then 08B and 02B only after the canary passes.

Preserve the current source/narration/audio/timing chains and v2.4 derived plans. Remove unsupported doorway/public-threshold leakage, add source-grounded promise/experience semantics and explicit transition states, then replay deterministic semantic/provider admission.

Provider requests and ceiling: 0; $0 / €0.

Success: validation, semantic quality, and provider readiness pass without source edits or episode-specific overrides; 03B retains source/audio identity and 57.8-second timing; the fix generalizes to 08B/02B.

Stop if two targeted fixes do not converge, more than three fixtures change, a gate would need weakening, or any provider/source/audio invalidation is proposed.

Authorization required: none.
