# YSAAS-015 content reuse

## Summary
Implemented clone, production templates, and shared asset reuse (US-048–US-050): domain policy matrix, persistence, API routes, OpenAPI/SDK lifecycle module.

## Changed files
- `packages/domain/src/content-reuse-contracts.ts`, `content-reuse-lifecycle.ts`, unit test
- `packages/persistence/src/postgres-content-reuse-repository.ts`, unit test
- `apps/api/src/postgres-api-content-reuse-use-cases.ts`, `http-server.ts`, `postgres-api-use-cases.ts`
- `apps/api/src/contracts/modules/lifecycle-paths.ts`, `openapi-components.ts`, `openapi-registry.ts`
- `packages/api-sdk/src/v1-contract/modules/lifecycle-operations.ts`, `registry.ts`

## Tests
- `pnpm test:focused -- packages/domain/src/content-reuse-lifecycle.unit.test.ts` — 6 passed
- `pnpm exec tsc -p packages/domain|persistence|api-sdk` — pass
- `apps/api` tsc — pass except known `job-process` / `provider-free-worker-entry` migrate errors

## Risks
- Asset lifecycle strings from DB must match domain enum; invalid values are skipped in search.
- Cross-project clone requires `shared` lifecycle or workspace sharing scope in provenance.

## Follow-up
- YSAAS-016 archive/restore; wire template binding into episode read model if BFF needs it.
