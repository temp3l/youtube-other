# Codex Run: API SDK

## Summary

Added a dependency-free TypeScript SDK for every current API operation. It includes typed resources and responses, Bearer authentication, request-id/idempotency/ETag helpers, RFC 9457 parsing and typed errors, abortable Retry-After job polling, validation cursor iteration, and rotating-secret webhook signature verification.

## Changed Files

- `packages/api-sdk/package.json`
- `packages/api-sdk/tsconfig.json`
- `packages/api-sdk/src/index.ts`
- `packages/api-sdk/src/index.unit.test.ts`
- `docs/reports/codex-runs/2026-08-01-api-sdk.md`

## Tests And Results

- `pnpm test:focused -- packages/api-sdk/src/index.unit.test.ts` — passed, 5 tests.
- `pnpm --filter @mediaforge/api-sdk typecheck` — passed after replacing an unavailable DOM `HeadersInit` alias with a package-owned compatible type.
- `git diff --check -- packages/api-sdk` — passed.

## Risks Remaining

The SDK is hand-maintained; OpenAPI compatibility checks must detect future server/SDK drift. No browser-bundle compatibility or published-package consumer test was run.

## Follow-up Tasks

Add the SDK to the public contract compatibility gate and compile a downstream consumer fixture.

## Commit Hash

Base: `69c62f2`; changes remain uncommitted.
