# Codex Run: API Task 12

## Summary

Added a tenant-scoped immutable storage adapter. Assets are written to quarantine, hash-verified, then atomically promoted; ready objects are inaccessible across workspaces. Legacy copies verify the inventoried hash before any registration.

## Changed Paths

- `packages/persistence/src/{tenant-object-storage,index,tenant-object-storage.unit.test}.ts`
- `docs/reports/codex-runs/2026-07-31-api-task-12.md`

## Tests

- Focused tenant-object-storage unit suite — passed (2 tests).
- `pnpm --filter @mediaforge/persistence typecheck` — passed.

## Commit Hash

Base: `5cf1262`; changes remain uncommitted.

## Unresolved Risks

This is a filesystem-backed contract adapter. Multipart transfers, signed URLs, retention jobs, durable migration inventory, and atomic database authority cutover need production object-store and migration wiring.
