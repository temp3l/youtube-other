Review exact target `hook-b04-deterministic-diagram.v4.png`; v3 and S02-B01 are comparison evidence only. Judge the pixels rather than the written intent, and fail closed if the causal relationship requires narration.

Check:

1. Is one parcel/payment order clearly entering the funnel?
2. Is the matching second parcel/payment order unmistakably outside and blocked by the wide grounded coral gate?
3. Does the gate read as a physical stop rather than decoration, furniture, or part of the funnel?
4. Does the reduced hand/arm avoid competing with the stopped order?
5. Is the accepted v3 output still clear: one dominant reservoir with nineteen drained units versus one isolated retained unit?
6. Does the muted image communicate within one to two seconds that another weak order is being prevented?
7. Is v4 materially distinct from S02-B01 and free of text, logos, UI, currency symbols, likeness, occupational drift, portrait dominance, or unsupported claims?

Return only JSON matching `expected-response.schema.json`. Approve only if every blocking criterion passes. Do not propose provider regeneration.
