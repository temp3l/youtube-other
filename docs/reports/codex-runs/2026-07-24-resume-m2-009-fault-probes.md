# Resume M2-009 fault probes

Status: `BLOCKED_NOT_ACCEPTED` (`INVALID_ARTIFACT`). Base/HEAD `934a40f`; dirty worktree preserved; no commit.

Changed: `packages/workflow-engine/src/artifact-repository.ts`, `packages/workflow-engine/src/workflow-store.unit.test.ts`, `packages/math-education/src/orchestration/canonical-task-adapters.ts`, and the M2-009 report.

Workspace: `/tmp/m2-009-continuation-p422nVtR`. Verifier failure reached the injected boundary and resumed. Interruption preserved completed stages. A truncated render was detected, replaced only after failed validation, and dependent manifests received current attempt lineage without rewriting valid binaries. An identical replay was fully cached. A valid release-revision change invalidated and rebuilt the affected graph.

Checks: workflow 10/10; chalk/renderer 2/2; runtime/media/localization 17/17; affected builds, typecheck, ESLint, and `git diff --check` passed. The consolidated math run passed 40/41; its remaining publish-fixture curriculum-identity mismatch also exists at base `934a40f`.

Zero-write paid-speech preflights estimated M5-ZO-001 at USD 0.224946 (7 calls), M5-GM-002 at USD 0.224475 (8), and M5-DZ-001 at USD 0.224484 (8): USD 0.673905 total. `paidProviderAuthorized` remained false; provider/network/upload/OAuth/publish/remote mutations and cost were zero.

Risk: all three historical pilot artifacts contain fixture tones. M2-009 requires explicit paid-speech approval with per-run ceilings at least equal to the estimates; M2-010A remains blocked on accepted M2-009 media.
