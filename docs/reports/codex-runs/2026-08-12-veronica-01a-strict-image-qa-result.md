# Veronica 01A strict image-QA result

Summary: adopted the current three pixel hashes and ran the strict no-generator QA path. S02-B01 was evaluated first and materially failed, so execution stopped before further calls. No image was generated.

Changed paths: canary adoption, strict-QA reservation/cost evidence, recensus generator, census, provisioning, ledger, tranche, implementation, and consolidated reports.

Result: semantic alignment 0.71, instant-read 0.62, narration support 0.74, and partial must-show coverage. The cost-station subtraction reads, but the single sale token, station separation, and retained remainder remain too abstract.

Provider: one successful `gpt-5.4-mini` response; 3,595 input and 867 output tokens; `$0.006598` / `€0.006289`. Reservation released; no retries.

Checks: source/pixel identities matched; strict JSON schema parsed; durable response/usage logs persisted; recensus invariants PASS.

Commit: none; HEAD `492543b`.

Risk: one bounded S02-B01 remediation regeneration is required; bulk generation remains prohibited.
