# Codex Run: API Task 08

## Summary

Added explicit workflow authority ownership. New relational runs default to `database-v1`; transitions require the owning authority. Legacy CLI migrations now carry `filesystem-legacy` authority, and a bounded policy models the one-window new-instance rollback switch.

## Changed Paths

- `packages/application/src/{errors,legacy-authority,index,contracts.unit.test}.ts`
- `packages/workflow-engine/src/caller-migration.ts`
- `apps/cli/src/production-caller-migration{,.unit.test}.ts`
- `packages/persistence/src/{relational-workflow-state,postgres-workflow-repository,relational-workflow-state.unit.test,postgres-workflow-repository.integration.test}.ts`
- `docs/reports/codex-runs/2026-07-31-api-task-08.md`

## Tests

- CLI migration focused suite — passed (17 tests).
- Local PostgreSQL persistence integration suite — passed (6 tests).
- `pnpm --filter @mediaforge/persistence typecheck` — passed.

## Commit Hash

Base: `5cf1262`; changes remain uncommitted.

## Unresolved Risks

Legacy command implementations still need gradual handler cutover; Task 09 supplies the shared API handlers needed for that migration.
