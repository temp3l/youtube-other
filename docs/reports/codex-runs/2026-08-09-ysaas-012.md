# YSAAS-012 codex run

## Changed files
- Domain: `webhook-contracts.ts`, `webhook-lifecycle.ts`, unit test
- Persistence: `postgres-webhook-signing-secret-repository.ts`, `listDeliveryAttempts` on webhook repository
- API: webhook use cases, endpoint service, `http-server` routes, OpenAPI/SDK webhook module

## Tests
- `pnpm test:focused -- packages/domain/src/webhook-lifecycle.unit.test.ts` — pass (4)
- Package typechecks — pass except pre-existing `job-process` / `provider-free-worker-entry`

## Risks
- Signing secrets encrypted with API cursor secret; worker must use `createPostgresWebhookSigningSecretResolver` for API-created endpoints
- BFF/UI surfaces not wired (API-only for this task)

## Follow-up
- YSAAS-015 clone/templates; wire webhook resolver in webhook worker process for API-created endpoints
