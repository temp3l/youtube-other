Summary: Ran a narrow manual smoke on `028-the-man-in-the-attic` for `stories production batch`, `stories batch todo`, `stories production repair --render`, and `stories render validate`. Fixed the shipped runtime by syncing the CLI/rendering `dist` artifacts and fixed `stories render validate` so `--episode` reaches the handler. The smoke now fails consistently on missing `state/story-workflow/workflows/*.json` manifests for the episode.
Changed paths: `apps/cli/src/story-render-command.ts`; `apps/cli/dist/story-{localization-commands,production-command,render-command,workflow-command-helpers}.{js,d.ts}`; `packages/rendering/dist/index.{js,d.ts}`; `docs/reports/2026-07-10/execution-checklist-implementation-report.md`; `docs/reports/codex-runs/2026-07-10-task-10-wrapper-manual-smoke.md`
Tests/checks: `pnpm mediaforge -- stories production batch --episode 028-the-man-in-the-attic --json`; `pnpm mediaforge -- stories batch todo --episode 028-the-man-in-the-attic --json`; `pnpm mediaforge -- stories production repair --episode 028-the-man-in-the-attic --render --json`; `pnpm mediaforge -- stories render validate --episode 028-the-man-in-the-attic --json`; package-local `tsc --noCheck` emits for `apps/cli` and `packages/rendering`
Commit hash: `9e3ba73`
Unresolved risks: wrapper commands still require persisted workflow manifests that real episodes in this workspace do not currently have; standard package builds remain blocked by unrelated dirty-tree type errors, so runtime `dist` had to be refreshed via `--noCheck`.
