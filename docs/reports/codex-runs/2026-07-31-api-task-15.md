# Codex Run: API Task 15

## Summary

Added an immutable publication-intent state machine. An intent can enter the irreversible execution state once; uncertain external outcomes terminate in `reconciliation_required` and cannot trigger another upload.

## Changed Paths

- `packages/application/src/{publication-safety,index,contracts.unit.test}.ts`
- `docs/reports/codex-runs/2026-07-31-api-task-15.md`

## Tests

- `pnpm --filter @mediaforge/application build` — passed.
- Focused application contracts suite — passed (8 tests).

## Commit Hash

Base: `5cf1262`; changes remain uncommitted.

## Unresolved Risks

The state machine still needs transactionally coupled approvals, credential/channel leases, provider evidence, and the fault-injection/reconciliation worker required before publication is enabled.
