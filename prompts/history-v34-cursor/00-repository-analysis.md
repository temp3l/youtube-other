# Cursor Phase 00 — Repository Analysis

## Mode

Use Cursor Plan mode.

Analysis only.

Do not edit:

- production code;
- schemas;
- tests;
- configuration;
- generated artifacts;
- extracted `episodes/*-v3.4/` reference packs.

Create only:

```text
reports/history-v34-pipeline-analysis.md
```

Keep the report under 2,500 words.

## Objective

Locate the exact repository code and verified commands that implement:

```text
narration
→ narration units
→ claims, entities, and qualifiers
→ visual purposes
→ maps, diagrams, and timelines
→ beats and shots
→ 16:9 and 9:16 plans
→ diagnostics and gate states
→ approval ZIP generation
```

## Required report

### 1. Canonical episode roots

For episodes 02–05 report:

- canonical root;
- canonical narration;
- canonical metadata;
- generated plan location;
- generated review-pack location;
- overlap or conflict with extracted `*-v3.4/` references.

### 2. Claims and semantic typing

List exact files, symbols, and call sequence for:

- narration segmentation;
- claim creation/import;
- temporal parsing;
- quantitative parsing;
- entity typing;
- geographic qualifiers;
- trust-attestation binding.

### 3. Visual modality pipeline

List exact files and symbols for:

- visual-purpose creation;
- modality selection;
- map intent and compilation;
- diagram proposal and graph validation;
- timeline creation;
- every silent map/diagram fallback.

### 4. Beats and shots

Report:

- semantic beat grouping;
- long-beat handling;
- shot-count decisions;
- shot grammar selection;
- repetition calculations;
- threshold enforcement.

### 5. Ratio planning

Report:

- 16:9 builder;
- 9:16 builder;
- retained/removed labels;
- collision/density checks;
- possible end-screen-safe extension point.

### 6. Approval gates

List exact code and blocker codes for:

- structural;
- editorial;
- content;
- production.

### 7. Semantic-provider boundary

Report:

- provider interface;
- no-web semantic mode;
- caching;
- credentials;
- deterministic validation after model output.

### 8. Verified commands

Provide existing exact commands for:

- generating one episode;
- generating one approval ZIP;
- running focused History tests;
- running generated-artifact tests;
- deterministic second generation;
- ZIP/checksum verification.

Do not invent missing commands.

### 9. Minimal defect-to-file map

Map each next-phase requirement to the smallest likely source files:

- Franklin maps and evidence graphic;
- Napoleon maps and logistics diagram;
- Rome and Black Death generalization;
- entity/qualifier correctness;
- beat and shot quality;
- ratio planning;
- artifact acceptance scripts;
- publishing output.

## Completion condition

The report contains repository-verified paths, symbols, and commands.

Do not implement anything in this phase.
