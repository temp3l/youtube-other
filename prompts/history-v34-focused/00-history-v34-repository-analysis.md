# Goal 00 — History V3.4 Repository Analysis

## Mode

Analysis only. Do not modify production code, generated artifacts, tests, or schemas.

Create only:

```text
reports/history-v34-pipeline-analysis.md
```

Keep it below 2,500 words.

## Objective

Identify the exact repository files, symbols, and call sequence that produce:

```text
narration
→ units
→ claims/entities/qualifiers
→ visual purposes
→ maps/diagrams/timelines
→ beats/shots
→ 16:9 and 9:16 plans
→ diagnostics/gates
→ approval ZIPs
```

The extracted directories under `episodes/*-v3.4/` are reference outputs only. Do not edit them.

## Required report

1. **Canonical episode roots**
   - actual source root for episodes 02–05;
   - narration path;
   - metadata path;
   - generated-plan path;
   - review-pack output path;
   - whether any extracted reference directory overlaps a canonical root.

2. **Claims and entities**
   - exact source files and exported functions/classes;
   - narration segmentation;
   - claim creation/import;
   - date/quantity/place classification;
   - trust-attestation binding.

3. **Visual modalities**
   - where modality selection happens;
   - where map, diagram, and timeline proposals compile;
   - where invalid graphics are rejected;
   - every location where map/diagram may silently fall back to archival imagery.

4. **Beats and shots**
   - beat grouping;
   - long-beat splitting;
   - shot-count selection;
   - shot-template selection;
   - repetition metrics and thresholds.

5. **Ratio planning**
   - 16:9 and 9:16 builders;
   - label retention/removal;
   - collision/density checks;
   - possible end-screen-safe layout hook.

6. **Gate calculation**
   - exact code for structural, editorial, content, and production readiness;
   - current blocker codes for each gate.

7. **OpenAI semantic boundary**
   - provider interface;
   - whether semantic structuring can run with web search disabled;
   - caching;
   - required credentials;
   - deterministic validators after model output.

8. **Exact commands**
   - one-episode plan generation;
   - review ZIP generation;
   - focused History tests;
   - deterministic second run;
   - ZIP integrity verification.
   Do not invent commands.

9. **Minimal defect-to-file map**
   - Franklin required maps;
   - Napoleon required maps/diagram;
   - typed entities/qualifiers;
   - beat/shot diversity;
   - ratio planning;
   - external acceptance scripts;
   - publishing metadata.

## Completion

Do not implement anything. The session is complete only when the report contains exact repository paths, symbols, and verified commands.
