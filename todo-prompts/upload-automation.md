Act as a senior TypeScript automation engineer specializing in the YouTube Data API v3, OAuth 2.0, resilient media uploads, and production-grade CLI tooling.

Your task is to inspect this repository and implement a complete, secure, type-safe YouTube upload pipeline for generated videos.

Do not replace working functionality unnecessarily. First inspect the repository, determine its package manager, module system, TypeScript configuration, existing episode structure, metadata format, logging conventions, and current generation workflow. Integrate with the existing architecture instead of creating a disconnected uploader.

## Primary objective

Implement an automated uploader that can:

1. Discover a completed episode.
2. Locate its rendered video.
3. Locate and validate its generated YouTube metadata.
4. Locate and validate its custom thumbnail.
5. Upload the video through the YouTube Data API v3.
6. Set all supported and relevant metadata.
7. Upload the custom thumbnail.
8. Optionally add the video to a playlist.
9. Verify that YouTube accepted the upload.
10. Write a machine-readable and human-readable upload report.
11. Prevent accidental duplicate uploads.

Use Node.js and strict TypeScript.

Use the official `googleapis` Node.js package for YouTube and OAuth integration.

## Before making changes

Inspect at least:

- `package.json`
- lock files
- `tsconfig*.json`
- `.env.example`
- existing source directories
- existing CLI scripts
- episode directories
- generated video files
- generated thumbnail files
- generated OpenAI metadata files
- existing logging and configuration utilities
- existing upload-related code
- repository documentation

Determine the actual repository conventions before deciding filenames or directories.

Do not expose, print, commit, copy, or include secret values in reports.

## Authentication

The user has already configured Google Cloud Console and stored credentials in `.env`.

Support these environment variables:

```dotenv
YOUTUBE_CLIENT_ID=
YOUTUBE_CLIENT_SECRET=
YOUTUBE_REFRESH_TOKEN=
YOUTUBE_REDIRECT_URI=http://localhost
YOUTUBE_CHANNEL_ID=
YOUTUBE_CHANNEL_ID_GERMAN=
YOUTUBE_CHANNEL_ID_SPANISH=
YOUTUBE_CHANNEL_ID_FRENCH=
```

Also recognize clearly named equivalent variables already used by the repository. Do not introduce duplicate environment-variable names without a migration reason.

Use OAuth2 authentication with the official Google client library.

Requirements:

- Initialize `google.auth.OAuth2`.
- Set the stored refresh token using `oauth2Client.setCredentials()`.
- Let the Google client refresh expired access tokens automatically.
- Never require a manually maintained access token for normal uploads.
- Never log OAuth credentials or tokens.
- Fail early with a useful error when required credentials are missing.
- Validate that the authenticated account owns or can upload to the configured channel.
- Resolve the expected channel from the upload language, compare it with the authenticated channel, and abort on mismatch.
- Provide a separate one-time authorization command only when no refresh token exists.

Implement an authorization command similar to:

```bash
npm run youtube:auth
```

It should:

1. Generate the Google OAuth consent URL.
2. Request the minimum required YouTube scopes.
3. Ask the operator to open the URL.
4. Accept the returned authorization code through a safe local CLI flow.
5. Exchange it for tokens.
6. Print the refresh token once, clearly marked for placement into `.env`.
7. Never automatically write secrets into tracked files.
8. Explain when Google does not return a new refresh token and how to re-consent using offline access and consent prompting.

Prefer scopes no broader than necessary. Use the upload/manage scope required for video upload, metadata management, thumbnail upload, and optional playlist insertion.

## Metadata input

Reuse the metadata already generated by OpenAI.

Search the repository for the actual metadata format. Support the existing format first.

If there is no stable schema, standardize on a JSON file such as:

```json
{
  "title": "Required video title",
  "description": "Required full YouTube description",
  "tags": ["tag one", "tag two"],
  "categoryId": "24",
  "defaultLanguage": "en",
  "defaultAudioLanguage": "en",
  "privacyStatus": "private",
  "publishAt": null,
  "madeForKids": false,
  "containsSyntheticMedia": true,
  "embeddable": true,
  "publicStatsViewable": true,
  "license": "youtube",
  "playlistId": null,
  "thumbnailPath": "./thumbnail.jpg",
  "videoPath": "./video.mp4",
  "notifySubscribers": false,
  "recordingDate": null,
  "chapters": [
    {
      "time": "00:00",
      "title": "Introduction"
    }
  ]
}
```

Adapt this schema to the repository’s existing naming conventions.

Use a runtime validation library already present in the repository. If none exists, add Zod.

Create a strict metadata schema and infer the TypeScript type from it. Do not maintain a separate handwritten interface that can drift from runtime validation.

Validate and normalize:

- title
- description
- tags
- category ID
- privacy status
- language codes
- scheduled publishing date
- made-for-kids setting
- thumbnail path
- video path
- playlist ID
- chapters
- boolean upload options

Do not silently truncate important fields unless the behavior is explicitly documented in the report.

For values exceeding YouTube limits:

- report the original length
- apply a deterministic and safe normalization where appropriate
- preserve complete metadata in the local report
- emit a warning
- reject values where truncation could change meaning or create malformed metadata

Remove:

- duplicate tags
- blank tags
- surrounding whitespace
- control characters
- malformed chapter entries

Preserve Unicode and localized metadata.

## OpenAI metadata compatibility

The uploader must not call OpenAI merely to upload a video when a valid generated metadata file already exists.

Implement a metadata adapter layer so the uploader can consume the current OpenAI-generated format and map it to the normalized YouTube schema.

Keep these responsibilities separate:

- OpenAI metadata generation
- metadata parsing and normalization
- YouTube upload execution

If metadata is missing, fail with a clear explanation by default.

Optionally support an explicit flag such as:

```bash
--generate-metadata
```

Only under that flag may the existing OpenAI metadata-generation service be invoked.

Do not embed prompts or OpenAI calls directly inside the YouTube API client.

## Description and chapters

When chapters are provided separately, append them to the description using valid timestamp lines.

Requirements:

- first chapter must begin at `00:00`
- timestamps must be sorted
- chapter titles must not be blank
- duplicate timestamps must be rejected
- existing chapter blocks in the description must not be appended twice
- preserve existing SEO text, disclaimers, credits, hashtags, and source information
- normalize newline handling across Linux, macOS, and Windows

Do not add invented claims, keywords, credits, or sources during upload.

## Video upload

Use:

```ts
youtube.videos.insert();
```

Upload the media as a readable file stream rather than loading the entire video into memory.

Set appropriate request parts, including the metadata sections actually used by the request.

Support relevant fields such as:

- `snippet.title`
- `snippet.description`
- `snippet.tags`
- `snippet.categoryId`
- `snippet.defaultLanguage`
- `snippet.defaultAudioLanguage`
- `status.privacyStatus`
- `status.publishAt`
- `status.selfDeclaredMadeForKids`
- `status.embeddable`
- `status.publicStatsViewable`
- `status.license`

Only send supported fields. Confirm field names against the installed Google API type definitions and current official API behavior.

Treat scheduled publishing safely:

- `publishAt` requires a valid future RFC 3339 timestamp
- scheduled videos must use the appropriate privacy status required by YouTube
- reject timestamps in the past
- log dates in ISO 8601
- never silently publish immediately when scheduling is invalid

Use `notifySubscribers` only when explicitly enabled. Default it to `false` for automated testing and repeated pipeline runs.

## Resumable uploads and reliability

Use the Google client library’s supported resumable media upload behavior where available.

The implementation must be suitable for large video files.

Add:

- upload progress reporting where supported
- retry handling for transient failures
- exponential backoff with jitter
- maximum retry attempts
- request timeouts
- classification of retryable and non-retryable errors
- useful diagnostics for quota, authentication, invalid metadata, upload-limit, and network errors

Retry only errors that are safe to retry, such as selected:

- connection resets
- timeouts
- HTTP 429
- transient HTTP 5xx responses

Do not endlessly retry:

- invalid credentials
- forbidden channel access
- invalid metadata
- unsupported media
- permanent quota or upload-limit errors

Never start a second upload merely because the post-upload thumbnail step failed.

Persist the returned video ID immediately after successful video insertion so later steps can resume.

## Thumbnail upload

After the video has been created, upload the custom thumbnail with:

```ts
youtube.thumbnails.set();
```

Validate before making the API request:

- file exists
- file is readable
- supported format
- MIME type matches content or file extension
- file is no larger than 2 MB

Support JPEG and PNG.

Use a readable stream.

A thumbnail failure must produce a partial-success result containing the uploaded video ID and a command that retries only the thumbnail step.

Provide a command such as:

```bash
npm run youtube:thumbnail -- --video-id VIDEO_ID --thumbnail path/to/thumbnail.jpg
```

Do not re-upload the video when retrying a thumbnail.

## Playlist support

When `playlistId` is present, add the uploaded video through the YouTube playlist-items API.

Playlist insertion must happen after a successful video upload.

Validate that the playlist is accessible to the authenticated account.

A playlist failure must not cause the video itself to be re-uploaded. Record partial success and provide a retry command that uses the existing video ID.

## Processing verification

After upload, optionally poll:

```ts
youtube.videos.list();
```

Request only the parts needed to verify:

- processing status
- upload status
- privacy status
- basic snippet metadata

Use bounded polling:

- configurable interval
- configurable timeout
- no infinite loop
- graceful handling when processing takes longer than the configured wait period

A processing timeout should not be reported as an upload failure when YouTube has already returned a valid video ID.

Report it as:

```text
uploaded_processing_pending
```

## Duplicate-upload protection

Implement robust duplicate prevention.

Before uploading, create an upload fingerprint based on stable inputs such as:

- absolute or normalized video path
- video file size
- SHA-256 hash of the rendered video
- channel ID
- normalized metadata identity

Store upload state locally in a generated state file, database, or repository-standard artifact directory.

Suggested states:

```text
validated
uploading
video_uploaded
thumbnail_uploaded
playlist_added
processing
completed
partial_failure
failed
```

Before uploading:

- check whether the exact video hash has already been uploaded to the configured channel
- if a completed record exists, abort and show the existing video ID and URL
- if a partial record exists, resume from the incomplete step
- require an explicit `--force` flag to upload the same artifact again
- make `--force` prominent in the report

Do not rely only on the video title for duplicate detection.

Write state atomically to avoid corruption after process interruption.

## CLI commands

Integrate with the existing package manager.

Provide commands equivalent to:

```bash
npm run youtube:validate -- --episode episodes/001-example
npm run youtube:upload -- --episode episodes/001-example
npm run youtube:upload -- --episode episodes/001-example --dry-run
npm run youtube:upload -- --episode episodes/001-example --privacy private
npm run youtube:upload -- --episode episodes/001-example --publish-at "2026-07-01T18:00:00Z"
npm run youtube:upload -- --episode episodes/001-example --force
npm run youtube:status -- --episode episodes/001-example
npm run youtube:resume -- --episode episodes/001-example
npm run youtube:thumbnail -- --video-id VIDEO_ID --thumbnail ./thumbnail.jpg
npm run youtube:auth
```

Use the repository’s actual package manager rather than blindly using npm.

The default upload privacy must be `private`, unless the repository already has an explicit safer configuration.

Publishing publicly or notifying subscribers must require explicit metadata or CLI configuration.

## Episode discovery

Support both:

```bash
--episode episodes/<episode-directory>
```

and explicit paths:

```bash
--video path/to/video.mp4
--metadata path/to/youtube-metadata.json
--thumbnail path/to/thumbnail.jpg
```

For episode-based uploads, inspect the repository and implement deterministic discovery rules.

Prefer explicit manifest references over filename guessing.

When multiple candidate videos, metadata files, or thumbnails exist, fail with a clear ambiguity error rather than choosing arbitrarily.

Support localized episode directories and Unicode filenames.

## Batch uploads

Implement optional sequential batch upload support:

```bash
npm run youtube:upload-batch -- --root episodes --status ready
```

Requirements:

- upload sequentially by default
- do not launch concurrent video uploads unless explicitly configured
- continue or stop based on an explicit `--continue-on-error` option
- never upload episodes not marked ready
- generate an aggregate batch report
- preserve per-episode reports
- skip already completed uploads
- resume partial uploads
- allow a configurable delay between uploads

Do not make batch upload the only available interface.

## Dry-run behavior

`--dry-run` must execute every local step except network mutations.

It should:

- load environment configuration
- verify credential presence without printing secrets
- discover files
- validate metadata
- validate video readability
- validate thumbnail size and type
- calculate the video hash
- construct and redact the intended YouTube request
- validate chapter formatting
- evaluate scheduling
- check local duplicate state
- show which API calls would be made

It must not:

- upload a video
- set a thumbnail
- add a playlist item
- modify remote YouTube data

## Logging

Use structured logging.

Log fields such as:

- operation
- episode ID
- upload state
- video path
- video size
- video hash
- YouTube video ID
- retry attempt
- elapsed time
- privacy status
- publish time
- partial-failure stage

Redact:

- client secret
- refresh token
- access token
- authorization code
- API authorization headers
- sensitive query parameters

Avoid logging the complete description at normal log levels.

Support readable console output and JSON logs if compatible with the existing logger.

## Upload reports

For every attempt, produce a machine-readable JSON report, for example:

```json
{
  "episodeId": "001-example",
  "status": "completed",
  "dryRun": false,
  "videoId": "youtube-video-id",
  "videoUrl": "https://www.youtube.com/watch?v=youtube-video-id",
  "privacyStatus": "private",
  "publishAt": null,
  "thumbnailUploaded": true,
  "playlistAdded": false,
  "processingStatus": "succeeded",
  "videoSha256": "hash",
  "startedAt": "ISO timestamp",
  "completedAt": "ISO timestamp",
  "warnings": [],
  "errors": []
}
```

Also produce a concise Markdown report suitable for reviewing a completed automation task.

Store reports in the repository’s existing output/report structure. If none exists, use a generated directory such as:

```text
<episode>/output/reports/youtube-upload.json
<episode>/output/reports/youtube-upload.md
```

Do not overwrite the history of failed attempts. Keep timestamped attempt records and optionally maintain one latest summary file.

## Error handling

Create typed application errors or discriminated error results.

Distinguish at least:

- configuration error
- authentication error
- authorization/channel mismatch
- metadata validation error
- missing artifact error
- thumbnail validation error
- duplicate upload error
- quota error
- transient network error
- video upload error
- thumbnail upload error
- playlist insertion error
- YouTube processing failure
- processing timeout
- interrupted upload
- unexpected internal error

Exit with a non-zero status for failed CLI operations.

Partial remote success must use a distinct exit code or clearly documented status so CI does not misinterpret it as total failure without preserving the uploaded video ID.

Never swallow exceptions.

## Security requirements

- Ensure `.env` is ignored by Git.
- Ensure generated token files are ignored by Git.
- Update `.env.example` with variable names only.
- Never include real credentials in documentation or tests.
- Never send credentials to OpenAI.
- Use least-privilege OAuth scopes.
- Use secure file permissions where token files are written.
- Avoid shell commands that interpolate untrusted paths.
- Validate all CLI input.
- Avoid command injection and path traversal.
- Do not permit an episode path to escape configured roots in batch mode.
- Redact secrets recursively from error objects before logging.
- Do not serialize the OAuth client object.

## Architecture

Keep responsibilities modular. A suitable design may include:

```text
youtube/
  auth/
    youtube-oauth-client.ts
    youtube-auth-command.ts
  metadata/
    youtube-metadata.schema.ts
    youtube-metadata-loader.ts
    openai-metadata-adapter.ts
    description-builder.ts
  upload/
    youtube-video-uploader.ts
    youtube-thumbnail-uploader.ts
    youtube-playlist-service.ts
    youtube-processing-service.ts
    upload-orchestrator.ts
  state/
    upload-state.schema.ts
    upload-state-store.ts
    upload-fingerprint.ts
  cli/
    youtube-upload.command.ts
    youtube-validate.command.ts
    youtube-resume.command.ts
    youtube-status.command.ts
  errors/
    youtube-upload.errors.ts
```

Adapt this to the project’s existing architecture. Do not introduce needless abstraction, dependency injection, or framework code when the repository uses a simple CLI structure.

Use:

- strict TypeScript
- explicit return types on exported functions
- `unknown` rather than `any`
- schema-derived types
- dependency injection where it materially improves testing
- readonly configuration
- abort signals where supported
- stream-based file handling
- atomic file writes
- testable clock, retry, and API dependencies

## Testing

Add automated tests without making real uploads.

At minimum, test:

- metadata schema success and failure
- OpenAI metadata mapping
- title, description, and tag normalization
- chapter validation and rendering
- future and past scheduling validation
- private-by-default behavior
- made-for-kids mapping
- missing credentials
- secret redaction
- channel mismatch handling
- file discovery
- ambiguous artifact discovery
- thumbnail type and size validation
- SHA-256 fingerprinting
- duplicate prevention
- atomic state persistence
- retry classification
- exponential-backoff bounds
- upload orchestration
- resume after video success and thumbnail failure
- resume after playlist failure
- dry-run performs no mutations
- uploaded video ID is persisted before subsequent API calls
- processing timeout is treated as pending rather than a lost upload
- malformed Google API responses
- partial-success report generation

Mock the Google API boundaries rather than mocking internal business logic excessively.

Do not contact YouTube or OpenAI during unit or integration tests.

## Documentation

Update the project README or create focused documentation covering:

1. Required Google Cloud configuration.
2. Required OAuth consent and scopes.
3. Required `.env` variables.
4. How to obtain a refresh token.
5. Expected video, thumbnail, and metadata structure.
6. Exact upload commands.
7. Dry-run usage.
8. Private, unlisted, public, and scheduled uploads.
9. Batch upload behavior.
10. Duplicate protection.
11. Resume behavior.
12. Thumbnail retry behavior.
13. Playlist behavior.
14. Quota and upload-limit errors.
15. Token revocation and reauthorization.
16. Security precautions.
17. How to inspect upload reports.
18. Troubleshooting channel mismatch.
19. Troubleshooting OAuth apps still in testing mode.
20. How to rotate or revoke credentials safely.

State clearly that publishing publicly is not the default.

## Implementation workflow

Perform the work now:

1. Inspect the repository.
2. Summarize the existing relevant architecture.
3. Identify the existing video, metadata, and thumbnail conventions.
4. State the implementation decisions based on that inspection.
5. Implement the uploader.
6. Add dependencies using the existing package manager.
7. Add validation and secure configuration.
8. Add CLI commands.
9. Add duplicate protection and resumable state.
10. Add tests.
11. Run formatting, linting, type checking, and relevant tests.
12. Fix all issues introduced by the implementation.
13. Run a full dry-run against one existing completed episode when available.
14. Do not perform a real YouTube upload unless an explicit repository configuration or command clearly authorizes it.
15. Produce a final execution report.

## Final response

At completion, report:

- files created
- files changed
- dependencies added
- commands added
- metadata format detected
- environment variables required
- test, lint, and type-check results
- dry-run result
- security protections implemented
- retry and resume behavior
- remaining manual Google/YouTube configuration
- any assumptions
- any unresolved risks

Do not merely provide example code or a proposed plan. Inspect and implement the solution in the repository.
