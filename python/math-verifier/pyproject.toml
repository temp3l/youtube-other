[project]
name = "mediaforge-math-verifier"
version = "2.0.0"
requires-python = ">=3.11,<3.15"
dependencies = ["sympy==1.14.0"]

[project.optional-dependencies]
test = ["pytest==8.4.1"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
